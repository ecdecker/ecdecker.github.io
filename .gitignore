# Hugo build output and lock file. The public/ directory is regenerated by
# `npm run site -- build` on demand and is not a versioned artifact.
/public/
/resources/_gen/
.hugo_build.lock
/artifacts/load-states/
/node_modules/
/.venv/
